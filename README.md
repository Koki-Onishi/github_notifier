# README

## Setup
```cassandraql
$ rbenv install 2.5.3
$ rbenv local 2.5.3
$ rbenv rehash
$ gem install bundler
$ bundle i --path vendor/bundle
$ bundle exec rake db:migrate
$ bundle exec rails c
```
